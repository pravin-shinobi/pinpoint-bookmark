import React from 'react'
import Login from './Login'

export const metadata={
    title:"Login",
    description:"Log in / Sign up to your account to maintain your bookmarks"
}

const page = () => {
  return (
    <div>
      <Login/>
    </div>
  )
}

export default page
